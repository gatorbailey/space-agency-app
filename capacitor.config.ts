import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.gatorbailey.spaceagency',
  appName: 'Space Agency',
  webDir: 'dist'
};

export default config;
